## Implementation Tasks

- [x] 1. Create capability spec `openspec/specs/esp32-s3-starter/spec.md`
- [x] 2. Implement scaffold under `ESP32/` (platformio.ini, src/main.cpp, README)
- [x] 3. Add tooling defaults to `platformio.ini` (upload/monitor ports & speeds)
- [x] 4. Validate spec (`openspec validate add-esp32-s3-starter --strict`)
- [x] 5. Build project (`pio run`) to confirm

Notes:
- All tasks were executed during this change; check individual files under `openspec/specs/esp32-s3-starter` and `ESP32/` for details.
