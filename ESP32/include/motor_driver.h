// motor_driver.h
// Deprecated compatibility header. Implementation moved to
// `servo_motor_driver.h` / `servo_motor_driver.cpp`.
#pragma once

#include "motor_drivers/servo_motor_driver.h"

// Note: keep this header as a thin compatibility wrapper so existing
// includes of "motor_driver.h" continue to work. Prefer `servo_motor_driver.h`.
